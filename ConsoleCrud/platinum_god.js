copy(Array.from(document.querySelectorAll('.ap-item')).concat(Array.from(document.querySelectorAll('.a-item'))).concat(Array.from(document.querySelectorAll('.rebirth-item'))).concat(Array.from(document.querySelectorAll('.rebirth-trinket'))).map(ap => ap.nextElementSibling).map(sib => [sib.querySelector('.item-title').textContent, sib.querySelector('.pickup').textContent, Array.from(sib.querySelectorAll(':scope > p:not([class])')).map(desc => desc.textContent), Array.from(sib.querySelectorAll('ul > p')).map(details => details.textContent), sib.querySelector('.r-unlock')?.textContent ?? ""]))